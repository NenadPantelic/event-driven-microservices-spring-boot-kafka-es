spring:
  application:
    name: reactive-elasticsearch-query-service
  profiles:
    active: reactive_elasticsearch_query
  cloud:
    config:
      name: reactive-elasticsearch-query-service,config-client
      uri: http://localhost:8888
      username: spring_cloud_user
      password: spring_cloud_password!

